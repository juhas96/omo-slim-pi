---
description: Pantheon workflow for implementation followed by review
---
Use the Pantheon workflow to implement, then review.

Recommended flow:
1. Explore as needed with `explorer`.
2. Implement with `fixer` or `designer` as appropriate.
3. Review with `oracle` before finalizing.
4. Summarize final verification.

User request:
$@
